import { expect } from 'chai';
import { program } from '@project-serum/anchor';

describe('paige_project', () => {
    it('should initialize', async () => {
        // Add your test logic here
        expect(true).to.be.true;
    });
});